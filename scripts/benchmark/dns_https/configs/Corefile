.:53 {
    bind 127.0.0.53
    errors
    health 127.0.0.53:18053
    prometheus 127.0.0.53:9153

    # Run CoreDNS with the repository root as its working directory.
    # Copy the committed seed zone here before each benchmark run so fault
    # injection never modifies a tracked file.
    file temp/dns-https-benchmark-runtime/db.dns-bench.test dns-bench.test {
        reload 1s
    }
}
